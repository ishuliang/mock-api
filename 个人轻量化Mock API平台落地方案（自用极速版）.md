# 个人轻量化Mock API平台落地方案（自用极速版）

# 一、方案总览\&个人版定位

## 1\.1 个人开发核心痛点

- 第三方在线Mock：公网延迟、免费额度限流、接口配置丢失、隐私数据上传外网、域名不稳定

- 本地代码Mock：耦合业务代码、删除繁琐、无法统一管理接口、无可视化配置、无接口文档

- 开源YApi/EasyMock：依赖MongoDB、部署笨重、冗余企业功能过多、启动耗资源、更新迭代麻烦

## 1\.2 个人版核心目标

**零复杂配置、1分钟Docker拉起、单文件数据库、无多余依赖、纯自用轻量化、可视化零代码配置、支持动态mock\+接口转发、本地/内网部署、改配置即时生效、极简更新迭代**，只服务个人前后端开发、自测、联调，无团队权限、无企业集成冗余功能。

## 1\.3 版本核心约束

- 部署：单机Docker单容器部署，无Redis、无Nginx、无额外中间件

- 开发：1天完成源码开发，后续改配置、改代码秒级更新

- 资源：内存占用≤50M，低配电脑、本地虚拟机均可运行

- 运维：零运维、自动备份、关闭所有企业级管控功能

---

# 二、极简技术架构（个人专属，极速开发）

## 2\.1 后端架构【最终选定】

框架：**Express \+ Node\.js**（最轻量、启动最快、代码极简、热更新友好）

- 数据库：SQLite 单文件数据库（无需安装服务，一个\.db文件存储全部数据，拷贝即迁移）

- 核心极简依赖：cors、json5、vm2（轻量化脚本沙箱）、sqlite3、dotenv

- 剔除依赖：JWT鉴权、OAuth、权限框架、Redis、日志集群、监控组件全部删除

- 核心优势：代码总量≤800行、本地热更新、Docker镜像体积＜80M、接口响应毫秒级

## 2\.2 前端架构（极简后台）

- 框架：Vue3 \+ Vite \+ NaiveUI（轻量化组件库，舍弃ElementPlus重型组件）

- 核心依赖：Monaco轻量化代码编辑器、axios，删除ECharts、审计、权限、成员管理组件

- 页面：仅保留首页、项目管理、接口编辑、请求日志4个页面

## 2\.3 部署架构

单Docker容器部署、宿主机端口直接映射、无反向代理、无集群、无环境多套隔离，单套开发环境够用；支持本地电脑常驻、开机自启容器。

---

# 三、个人版MVP核心功能（砍掉全部非必要功能）

## 3\.1 必做核心功能（自用全覆盖）

### 3\.1\.1 极简项目\+接口管理

1. 项目管理：新增/删除/重命名项目，无归档、无分组、无成员管理

2. 接口管理：GET/POST/PUT/DELETE 主流请求方式、自定义路由、接口名称备注

3. 双模式切换：Mock模拟数据模式 / 代理转发真实后端接口模式（个人联调刚需）

### 3\.1\.2 个人开发刚需Mock能力

1. 静态Mock：JSON固定响应，粘贴即用

2. 内置动态变量：随机ID、手机号、时间戳、数字、空数组、分页数据（个人开发高频模板）

3. 基础异常模拟：自定义响应延时、500/401/403状态码、超时模拟

4. 参数匹配：简单Query/Body参数匹配，不同参数返回不同数据

5. 全局自动跨域：默认开启CORS，前端本地开发无需配置代理

### 3\.1\.3 极简调试\&日志

- 内置在线接口调试器，脱离Postman快速自测

- 简易请求日志：记录请求时间、入参、响应体，自动7天清理日志

- 自动极简接口文档：路由、请求方式、响应示例一键复制

### 3\.1\.4 自用加分功能

- 轻量JS脚本沙箱：简单JS逻辑动态生成数据，禁用高危API，安全无风险

- 接口配置一键复制、项目配置一键克隆

- OpenAPI/Swagger接口一键导入（对接后端接口文档，快速生成mock）

## 3\.2 彻底删除功能（个人版无用）

团队协作、角色权限、SSO登录、审计日志、流量监控、IP限流、版本回溯、WebSocket、离线包、SDK生成、多环境隔离、钉钉/企业微信对接、压测适配全部删除。

---

# 四、数据库极简表设计（仅4张表，单文件SQLite）

剔除全部冗余字段，无关联复杂设计，适配个人使用，文件可直接拷贝备份

1. **project 项目表**：id、项目名称、项目描述、创建时间

2. **api\_info 接口主表**：id、项目id、接口名称、请求路径、请求方法、是否启用、响应延时、默认状态码

3. **api\_mock 配置表**：id、接口id、静态json、动态js脚本、代理转发地址、参数匹配规则

4. **api\_log 请求日志表**：id、接口id、请求参数、请求时间、响应耗时（自动过期清理）

---

# 五、开发\+部署\+更新流程（极速落地）

## 5\.1 开发工期：单日完工

- 上午：后端脚手架、SQLite建表、Mock核心接口、代理转发逻辑开发

- 下午：前端页面开发、联调、功能自测、Docker镜像打包

- 夜间：本地部署验证，整体耗时≤8小时

## 5\.2 极速部署（1分钟上线）

单Docker命令部署，无compose、无环境变量复杂配置、无数据库初始化脚本：

docker run \-d \-p 8100:8100 \-\-name personal\-mock personal\-mock:latest

本地浏览器访问：http://localhost:8100 直接使用

## 5\.3 快速更新方案（适配个人迭代）

1. 配置更新：页面修改Mock参数、响应体，**秒级即时生效**，无需重启服务

2. 代码小更新：本地修改后端/前端源码，执行打包脚本，30s重新构建镜像、替换容器

3. 版本迭代：保留SQLite数据文件，容器重建数据不丢失，无缝升级

---

# 六、轻量化安全\&运维（零运维设计）

## 6\.1 个人版极简安全

- 内网/本地部署，不暴露公网，关闭外网访问，无需接口鉴权

- 轻量化VM2沙箱隔离自定义JS脚本，防止脚本篡改本地文件

- 自动脱敏Mock模拟业务数据，无真实业务隐私数据存储

## 6\.2 自动化运维

- SQLite单文件自动备份，每日生成备份快照

- 请求日志自动7天过期清理，避免磁盘占用

- Docker容器开机自启、异常自动重启

- 无需监控、无需告警、无需中间件维护

---

# 七、个人版对比市面产品优势

|方案|自研个人版Mock|YApi|在线第三方Mock|
|---|---|---|---|
|部署耗时|1分钟Docker启动|30分钟\+依赖安装|依赖公网网络|
|资源占用|内存≤50M，极低开销|内存≥500M，笨重|限流、延迟高|
|冗余功能|全部删减，纯开发刚需|大量团队/企业冗余功能|广告、会员限制|
|更新迭代|源码极简，自定义修改超快|源码臃肿，二次开发困难|无法自定义修改|

---

# 八、个人自用极简拓展（可选）

- 可选1：本地桌面端封装，脱离Docker后台运行

- 可选2：新增常用业务Mock模板（用户、订单、列表数据）一键套用

- 可选3：支持Mock数据JSON一键导出本地文件备份

> （注：文档部分内容可能由 AI 生成）
